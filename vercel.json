{
  "version": 2,
  "buildCommand": "cd backend-node && npm install",
  "startCommand": "cd backend-node && node server.js",
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend-node/server.js" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}